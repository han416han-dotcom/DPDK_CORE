# DPDK Core Analyzer — 部署指南

> 适用场景：将项目从 Windows 开发环境迁移到 Linux 服务器

---

## 目录

- [一、环境要求](#一环境要求)
- [二、服务器依赖安装](#二服务器依赖安装)
- [三、数据库配置](#三数据库配置)
- [四、项目构建（Windows 开发机执行）](#四项目构建windows-开发机执行)
- [五、文件传输](#五文件传输)
- [六、服务启动与停止](#六服务启动与停止)
- [七、验证部署](#七验证部署)
- [八、常见问题](#八常见问题)

---

## 一、环境要求

### 1.1 Windows 开发机（用于构建）

| 软件 | 版本要求 | 验证命令 |
|------|---------|---------|
| JDK | 17+ | `java -version` |
| Maven | 3.8+ | `mvn -version` |
| Node.js | 18+ | `node -v` |
| npm | 9+ | `npm -v` |

### 1.2 Linux 服务器（用于运行）

| 软件 | 版本要求 | 验证命令 |
|------|---------|---------|
| JDK | 17+ | `java -version` |
| MySQL | 8.x | `mysql --version` |
| Python | 3.10+ | `python3 --version` |
| pip | 21+ | `pip3 --version` |
| GDB | 12+ | `gdb --version` |

---

## 二、服务器依赖安装

### 2.1 基础软件（CentOS/Rocky Linux）

```bash
# JDK 17
yum install -y java-17-openjdk-devel

# MySQL 8
yum install -y mysql-server
systemctl enable mysqld
systemctl start mysqld

# Python + pip
yum install -y python3 python3-pip

# GDB
yum install -y gdb

# 工具
yum install -y curl lsof
```

### 2.2 基础软件（Ubuntu/Debian）

```bash
# JDK 17
apt update
apt install -y openjdk-17-jdk

# MySQL 8
apt install -y mysql-server
systemctl enable mysql
systemctl start mysql

# Python + pip
apt install -y python3 python3-pip python3-venv

# GDB
apt install -y gdb

# 工具
apt install -y curl lsof
```

### 2.3 Python 依赖

所有 Python 脚本和依赖已打包在 jar 中，但运行时仍需要系统安装 `pyelftools`：

```bash
# 方式一：系统级安装
pip3 install pyelftools>=0.31

# 方式二：虚拟环境
python3 -m venv /opt/dpdk-core/venv
source /opt/dpdk-core/venv/bin/activate
pip install pyelftools>=0.31
deactivate
```

如使用虚拟环境，需将 `application-prod.yml` 中 `app.python.cmd` 改为虚拟环境的 python 路径：

```yaml
app:
  python:
    cmd: /opt/dpdk-core/venv/bin/python3   # 默认是 python3
```

### 2.4 创建部署目录

```bash
mkdir -p /opt/dpdk-core/uploads
```

### 2.5 开启 core dump 支持（可选）

```bash
# 临时生效（当前会话）
ulimit -c unlimited

# 永久生效
echo "root soft core unlimited" >> /etc/security/limits.conf
echo "root hard core unlimited" >> /etc/security/limits.conf
echo "* soft core unlimited" >> /etc/security/limits.conf
echo "* hard core unlimited" >> /etc/security/limits.conf

# core dump 路径
echo "/tmp/core.%p.%t" > /proc/sys/kernel/core_pattern

```

---

## 三、数据库配置

### 3.1 创建数据库

```bash
mysql -u root -p
```

```sql
CREATE DATABASE IF NOT EXISTS dpdk_core DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'root';
FLUSH PRIVILEGES;
```

### 3.2 验证数据库

```bash
mysql -u root -proot -e "SHOW DATABASES;"
```

应看到 `dpdk_core` 数据库。

> 注：表结构由 Spring Boot 启动时通过 `schema.sql` 自动创建，无需手动导入。

---

## 四、项目构建（Windows 开发机执行）

### 4.1 构建完整 jar

```powershell
cd C:\IdeaProjects\DPDK_CORE
mvn clean package -DskipTests
```

构建过程会自动：
1. 安装前端 npm 依赖
2. 构建 Vue 前端（输出到 `frontend/dist/`）
3. 将前端构建产物复制到 `static/`
4. 复制 Python 脚本到 `scripts/`
5. 复制 GDB 脚本到 `gdb/`
6. 打包为 Spring Boot fat jar

### 4.2 确认构建产物

```powershell
# 确认 jar 大小（约 30MB）
ls target/dpdk-core-analyzer-1.0.0.jar

# 确认前端静态文件在 jar 内
jar tf target/dpdk-core-analyzer-1.0.0.jar | findstr "static/index.html"
jar tf target/dpdk-core-analyzer-1.0.0.jar | findstr "favicon"

# 确认 Python 脚本在 jar 内
jar tf target/dpdk-core-analyzer-1.0.0.jar | findstr "scripts/parse_gdb_log.py"
```

---

## 五、文件传输

### 5.1 XFTP 传输

使用 XFTP 连接服务器 `192.168.188.99`（root 用户），将以下文件从 Windows 拖到 Linux：

| 源路径（Windows） | 目标路径（Linux） |
|-----------------|-----------------|
| `C:\IdeaProjects\DPDK_CORE\target\dpdk-core-analyzer-1.0.0.jar` | `/opt/dpdk-core/dpdk-core-analyzer-1.0.0.jar` |

**只需要传输这一个 jar 文件即可。** Python 脚本、GDB 脚本、前端页面均已打包在 jar 内，无需单独传输。

> 如果服务器上此前已有旧 jar，传输前建议先删除旧文件：
> ```bash
> rm -f /opt/dpdk-core/dpdk-core-analyzer-1.0.0.jar
> ```

### 5.2 确认传输

```bash
ls -lh /opt/dpdk-core/dpdk-core-analyzer-1.0.0.jar
# 应显示约 30MB
```

---

## 六、服务启动与停止

### 6.1 启动服务

```bash
cd /opt/dpdk-core

nohup java -jar dpdk-core-analyzer-1.0.0.jar \
  --spring.profiles.active=prod \
  > app.log 2>&1 &

# 等待 3~5 秒后检查日志
tail -f app.log
```

看到以下日志说明启动成功：

```
Tomcat started on port 8080 (http) with context path ''
Started DpdkCoreApplication in X.XXX seconds
```

### 6.2 停止服务

```bash
kill -9 $(pgrep -f dpdk-core-analyzer)
```

### 6.3 重启服务（完整流程）

```bash
# 1. 停止旧进程
kill -9 $(pgrep -f dpdk-core-analyzer)

# 2. 确认已停止
ps aux | grep dpdk-core-analyzer
# 应只显示 grep 进程

# 3. 删除旧日志（可选）
rm -f /opt/dpdk-core/app.log

# 4. 启动新进程
nohup java -jar /opt/dpdk-core/dpdk-core-analyzer-1.0.0.jar \
  --spring.profiles.active=prod \
  > /opt/dpdk-core/app.log 2>&1 &

# 5. 查看启动日志
tail -f /opt/dpdk-core/app.log
```

### 6.4 生产环境建议（systemd 服务）

创建服务文件 `/etc/systemd/system/dpdk-core.service`：

```ini
[Unit]
Description=DPDK Core Analyzer
After=network.target mysql.service

[Service]
Type=simple
User=root
WorkingDirectory=/opt/dpdk-core
ExecStart=/usr/bin/java -jar /opt/dpdk-core/dpdk-core-analyzer-1.0.0.jar --spring.profiles.active=prod
Restart=on-failure
RestartSec=10
StandardOutput=append:/opt/dpdk-core/app.log
StandardError=append:/opt/dpdk-core/app.log

[Install]
WantedBy=multi-user.target
```

启用：

```bash
systemctl daemon-reload
systemctl enable dpdk-core
systemctl start dpdk-core
systemctl status dpdk-core
```

---

## 七、验证部署

### 7.1 验证服务运行

```bash
# 检查进程
ps aux | grep dpdk-core-analyzer

# 检查端口
lsof -i :8080

# 检查日志
tail -20 /opt/dpdk-core/app.log
```

### 7.2 验证静态资源

```bash
# favicon
curl http://localhost:8080/favicon.svg

# 前端页面
curl -s http://localhost:8080/ | head -5
```

favicon 应返回 SVG 内容（蓝色方块图标），前端页面应返回 HTML。

### 7.3 验证 API

```bash
# 文件上传
curl -s -F "file=@/bin/ls" -F "fileType=EXECUTABLE" http://localhost:8080/api/files/upload

# 任务列表
curl -s http://localhost:8080/api/tasks/list
```

### 7.4 验证自动解析（需有 core dump）

```bash
# 上传 core dump
curl -s -F "file=@/tmp/core.xxx" -F "fileType=CORE_DUMP" http://localhost:8080/api/files/upload

# 上传可执行文件
curl -s -F "file=@/tmp/test_crash" -F "fileType=EXECUTABLE" http://localhost:8080/api/files/upload

# 创建解析任务（以实际返回的 fileId 为准）
curl -s -X POST http://localhost:8080/api/tasks/create \
  -H "Content-Type: application/json" \
  -d '{"gdbLogFileId": 1, "execFileId": 2, "taskName": "验证测试"}'
```

---

## 八、常见问题

### 8.1 数据库连接失败

**现象**：启动日志中有 `CommunicationsException` 或 `Access denied`。

**检查**：
```bash
# MySQL 是否在运行
systemctl status mysql

# 用户名密码是否正确
mysql -u root -proot -e "SELECT 1;"

# 数据库是否存在
mysql -u root -proot -e "SHOW DATABASES;" | grep dpdk_core
```

**修复**：
```sql
-- 重置密码
ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'root';
FLUSH PRIVILEGES;
```

### 8.2 GDB 解析失败

**现象**：任务状态 `FAILED`，日志中有 `CoreDumpProcessor` 错误。

**检查**：
```bash
# GDB 是否安装
gdb --version

# core dump 是否可读
file /tmp/core.xxx
```

### 8.3 Python 脚本执行失败

**现象**：日志中有 `PythonExecutor` 错误。

**检查**：
```bash
# Python3 是否安装
python3 --version

# pyelftools 是否安装
python3 -c "import elftools; print('ok')"

# 如未安装
pip3 install pyelftools>=0.31
```

### 8.4 前端页面空白

**现象**：访问 `http://192.168.188.99:8080/` 页面空白或报错。

**检查**：
```bash
# 确认 jar 内包含前端文件
jar tf /opt/dpdk-core/dpdk-core-analyzer-1.0.0.jar | grep "static/index.html"

# 确认不是浏览器缓存（Ctrl+F5 强制刷新或开无痕模式）
```

### 8.5 上传文件过大

**配置限制**（`application-prod.yml`）：
- 单文件最大 1GB
- 单次请求最大 2GB

**磁盘空间**：
```bash
# 检查上传目录空间
df -h /opt/dpdk-core/uploads
```

---

## 关键路径速查

| 项目 | 路径 |
|------|------|
| 部署目录 | `/opt/dpdk-core/` |
| jar 文件 | `/opt/dpdk-core/dpdk-core-analyzer-1.0.0.jar` |
| 上传目录 | `/opt/dpdk-core/uploads/` |
| 应用日志 | `/opt/dpdk-core/app.log` |
| MySQL 地址 | `localhost:3306` |
| 数据库名 | `dpdk_core` |
| 数据库用户 | `root` |
| 数据库密码 | `root` |
| 服务端口 | `8080` |
| API 前缀 | `/api` |
| 前端入口 | `http://192.168.188.99:8080/` |

---

*本文档由 opencode 根据项目源码与部署实践生成。*
