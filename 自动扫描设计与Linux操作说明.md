# 自动扫描设计与 Linux 操作说明

## 1. 目标

这一步的目标是把 `core / gdb log / ELF` 三类文件组织成一套稳定的自动扫描链路：

- 固定扫描根目录：`/home/hnhyh/workspace/core`
- 自动识别 `CORE_DUMP` 和 `GDB_LOG`
- 自动寻找对应 ELF
- 自动把本地文件注册进平台
- 自动创建解析任务

这套方案的核心思想是：

1. `core` 和 `log` 固定落在同一个扫描根目录下
2. ELF 允许放在多个搜索目录里，并支持多级递归查找
3. 若 GDB 日志中已经包含 ELF 路径，优先使用日志里的绝对路径
4. 若日志里没有路径，再退回到“文件名归一化匹配”

## 2. 代码设计

### 新增配置

当前在 `application.yml` 中增加了：

```yml
app:
  auto-scan:
    enabled: true
    scan-root: /home/hnhyh/workspace/core
    max-depth: 6
    header-probe-lines: 80
    elf-search-roots:
      - .
      - elf
      - bin
      - build
      - target
```

含义如下：

- `scan-root`
  - 扫描根目录，`core / log` 都从这里往下递归找
- `max-depth`
  - 最大递归深度，避免扫太深
- `header-probe-lines`
  - 读取 GDB 日志前多少行做路径提示提取
- `elf-search-roots`
  - ELF 搜索根目录列表
  - 相对路径会自动拼接到 `scan-root`

### 新增后端能力

- `AutoScanService`
  - 扫描固定目录
  - 生成候选源文件列表
  - 自动匹配 ELF
  - 将本地文件注册为 `upload_files`
  - 调用现有任务服务创建解析任务

- `AutoScanController`
  - `GET /api/scan/candidates`
  - `POST /api/scan/create-task`

### 新增本地文件注册方式

原本平台只支持前端上传 `MultipartFile`。  
这次增加了“注册本地已存在文件”的能力：

- 不再复制本地 `core / log / elf`
- 直接把它们原始绝对路径写入 `upload_files.storage_path`
- 避免大 core 文件重复拷贝

这对自动扫描场景尤其重要，因为 core 文件可能非常大。

## 3. ELF 自动匹配思路

### 第一优先级：GDB 日志里的绝对路径

如果是 GDB 日志，扫描器会优先从日志头部提取：

- `Reading symbols from /path/to/app...`
- `Core was generated by '/path/to/app ...'`

如果这个路径真实存在，并且文件类型确实是可执行 ELF / 共享对象，就直接用它。

这是最可靠的匹配方式。

### 第二优先级：文件名归一化匹配

如果日志里没有明确路径，就退回到文件名规则。

例如：

- `core.test_app.12345` -> `test_app`
- `test_app__12345__1714380000.core` -> `test_app`
- `test_app__20260429_103000.log` -> `test_app`
- `test_app_gdb.log` -> `test_app`

然后把 ELF 文件名也做同样的归一化，并按以下规则打分：

1. 日志提示文件名精确相等
2. 归一化名称精确相等
3. 前缀匹配
4. 包含匹配
5. 同目录额外加分

得分最高且超过阈值时，视为自动匹配成功。

### 为什么这样设计

因为：

- GDB 日志有机会直接暴露 ELF 路径
- core 文件本身通常很难单靠文件内容直接稳定反推 ELF 路径
- 所以真正可落地的方案一定是“路径提示优先，命名约定兜底”

## 4. ELF 应该放在哪里

### 推荐方案 A：固定根目录下单独建 ELF 目录

```text
/home/hnhyh/workspace/core/
├─ logs/
├─ cores/
├─ elf/
│  ├─ test_app/
│  │  └─ test_app
│  ├─ dpdk_app/
│  │  └─ dpdk_app
```

优点：

- 结构清晰
- 自动扫描稳定
- 适合长期归档

### 推荐方案 B：按案例建目录

```text
/home/hnhyh/workspace/core/
├─ case-001/
│  ├─ test_app__20260429_103000.log
│  ├─ test_app__12345__1714380000.core
│  └─ test_app
├─ case-002/
│  ├─ dpdk_ring__20260429_104500.log
│  ├─ dpdk_ring__22345__1714381000.core
│  └─ dpdk_ring
```

优点：

- 一个案例天然聚在一起
- 同目录加分匹配成功率高

### 是否支持多级目录自动查找

支持。

当前代码会在这些根下递归查找 ELF：

- `/home/hnhyh/workspace/core`
- `/home/hnhyh/workspace/core/elf`
- `/home/hnhyh/workspace/core/bin`
- `/home/hnhyh/workspace/core/build`
- `/home/hnhyh/workspace/core/target`

所以 ELF 可以放在多级文件夹里。

## 5. 推荐命名规则

为了让自动匹配更稳定，建议把 ELF basename 作为所有文件的主前缀。

### ELF

```text
test_app
dpdk_ring
mbuf_double_free
```

### GDB 日志

```text
test_app__20260429_103000.log
dpdk_ring__20260429_104500.log
mbuf_double_free__20260429_110000.log
```

### Core 文件

```text
test_app__12345__1714380000.core
dpdk_ring__22345__1714381000.core
mbuf_double_free__32345__1714382000.core
```

命名规则建议：

1. 第一段永远是 ELF basename
2. 第二段可以是 pid
3. 第三段可以是 unix 时间戳

这样后端做归一化后，很容易把三者配到一起。

## 6. Linux 上如何固定生成到指定目录

### 第一步：准备目录

```bash
mkdir -p /home/hnhyh/workspace/core/{logs,cores,elf}
```

### 第二步：允许生成 core

```bash
ulimit -c unlimited
```

如果希望 core 更小，仍然建议：

```bash
echo 0x1 > /proc/self/coredump_filter
```

### 第三步：固定 core 输出目录和命名

```bash
sudo sysctl -w kernel.core_pattern=/home/hnhyh/workspace/core/cores/%e__%p__%t.core
```

其中：

- `%e`：可执行文件名
- `%p`：进程 pid
- `%t`：unix 时间戳

这样 core 会自动变成：

```text
/home/hnhyh/workspace/core/cores/test_app__12345__1714380000.core
```

### 第四步：把 ELF 固定放到可扫描位置

例如：

```bash
mkdir -p /home/hnhyh/workspace/core/elf/test_app
cp ./test_app /home/hnhyh/workspace/core/elf/test_app/test_app
```

### 第五步：生成和 ELF 对应的日志

```bash
APP=test_app
CORE=/home/hnhyh/workspace/core/cores/${APP}__12345__1714380000.core
ELF=/home/hnhyh/workspace/core/elf/${APP}/${APP}
LOG=/home/hnhyh/workspace/core/logs/${APP}__$(date +%Y%m%d_%H%M%S).log

gdb -batch \
  -x /path/to/generate_dpdk_core_log.gdb \
  "$ELF" "$CORE" > "$LOG"
```

这样：

- 日志名带 ELF basename
- core 名带 ELF basename
- ELF 本身也在扫描路径中

自动匹配会很稳。

## 7. 最推荐的 Linux 落地约定

我建议你在 Linux 上统一成：

### 目录

```text
/home/hnhyh/workspace/core/
├─ logs/
├─ cores/
├─ elf/
```

### core_pattern

```bash
sudo sysctl -w kernel.core_pattern=/home/hnhyh/workspace/core/cores/%e__%p__%t.core
```

### 日志命名

```text
/home/hnhyh/workspace/core/logs/%e__YYYYmmdd_HHMMSS.log
```

### ELF 命名

```text
/home/hnhyh/workspace/core/elf/<app>/<app>
```

这样扫描器会从三条线索里命中：

1. `%e` 命名
2. 同名 stem
3. GDB 日志头部路径

## 8. 什么时候仍建议人工指定 ELF

以下情况建议传 `execPath`：

1. `core / log` 名字和 ELF 完全不相关
2. 日志头部没有 `Reading symbols from ...`
3. 同一个扫描范围内存在多个近似同名 ELF
4. ELF 存在于扫描根之外且日志也没有记录绝对路径
5. 你保留了多个 strip / 非 strip 版本，名字又很像

## 9. 当前实现边界

当前已经支持：

- 固定目录递归扫描
- GDB 日志头部提取 ELF 路径
- 多级目录 ELF 搜索
- 文件名归一化匹配
- 本地文件注册而不复制
- 自动创建任务

当前还没有做：

- 从 core 内部抽取 `build-id` 再反查 ELF
- inotify 持续监听固定目录
- 前端候选列表页面

如果下一步继续做，我建议优先级：

1. 前端增加“扫描候选列表”
2. 支持“确认匹配后创建任务”
3. 再做 inotify 监听，让目录变化时自动入队
